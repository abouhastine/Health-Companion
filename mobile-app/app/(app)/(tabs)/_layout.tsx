import { Ionicons } from '@expo/vector-icons'; import { Tabs } from 'expo-router';
const icons: Record<string, keyof typeof Ionicons.glyphMap> = { home: 'home-outline', care: 'search-outline', appointments: 'calendar-outline', results: 'document-text-outline', assistant: 'sparkles-outline' };
export default function TabsLayout() { return <Tabs screenOptions={({ route }: { route: { name: string } }) => ({ headerTitle: 'Health Companion', tabBarActiveTintColor: '#0f766e', tabBarIcon: ({ color, size }: { color: string; size: number }) => <Ionicons name={icons[route.name] ?? 'ellipse-outline'} size={size} color={color} /> })}><Tabs.Screen name="home" options={{ title: 'Home' }} /><Tabs.Screen name="care" options={{ title: 'Find care' }} /><Tabs.Screen name="appointments" options={{ title: 'Appointments' }} /><Tabs.Screen name="results" options={{ title: 'Results' }} /><Tabs.Screen name="assistant" options={{ title: 'Assistant' }} /></Tabs>; }
